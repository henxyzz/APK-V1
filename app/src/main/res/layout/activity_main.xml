<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="WiFi Scanner"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:paddingBottom="20dp"/>

    <Button
        android:id="@+id/btnScanWiFi"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Scan WiFi"
        android:layout_below="@id/tvTitle"
        android:layout_marginTop="10dp"/>

    <Button
        android:id="@+id/btnChangeDNS"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ganti DNS (1.1.1.1)"
        android:layout_below="@id/btnScanWiFi"
        android:layout_marginTop="10dp"/>

    <Button
        android:id="@+id/btnFlashlight"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Flashlight Disco"
        android:layout_below="@id/btnChangeDNS"
        android:layout_marginTop="10dp"/>
</RelativeLayout>